spring:
  datasource:
    url: jdbc:mysql://localhost:3306/eda_kotlin?serverTimezone=Asia/Seoul
    username: sgs1159
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    hibernate:
      ddl-auto: update
    defer-datasource-initialization: true
  data:
    mongodb:
      uri: mongodb://rootuser:1234@localhost:27018/mongo_eda_kotlin?authSource=admin
    elasticsearch:
      host: localhost
      port: 9200
  kafka:
    bootstrap-servers: localhost:9092,localhost:9093,localhost:9094
external-server:
  metadata:
    url: http://localhost:8088